#include <stdio.h>

int main() {
    int a[5];
    int i,n,s=0;
    printf("enter elements size");
    scanf("%d",&n);
    printf("enter elements");
    for(i=0;i<n;i++)
    {
    scanf("%d",&a[i]);
    }
    for(i=(n-1);i>-1;i--)
    {
    
    
    printf("\n%d",a[i]);
    
    
    }
    return 0;
}
