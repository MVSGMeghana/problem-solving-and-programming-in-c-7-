#include <stdio.h>

int main() 
{
    int n;
float marks[5],sum,avg;
printf("enter the no.of elements");
scanf("%d",&n);
printf("enter sub marks");
for(int i=0;i<n;i++)
{
scanf("%f",&marks[i]);
sum +=marks[i];
}
printf("%.0f",sum);
avg=sum/n;
printf("\n%.2f",avg);
}
