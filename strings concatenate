#include <stdio.h>
#include <string.h>

int main()
{
    char a[20],b[20];
    int x;
    printf("enter string");
    gets(a);
    printf("enter string");
    gets(b);
    puts(a);
    puts(b);
    
    strcat(a,b);
   printf("new values are\n");
   puts(a);
   puts(b);
    return 0;
}
