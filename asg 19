#include <stdio.h>
int main()
{
char ch;
printf("enter the number");
scanf("%c",&ch);
switch(ch)
{
case '-':
printf("negative number");
break;
default:
// ch doesnt match any case constant '-'
printf("positive number");
}
return 0;
}
output
enter the number-65
negative number
